spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://ff-db.c9ajyqrn8o5x.sa-east-1.rds.amazonaws.com:3306/ff_db?useTimezone=true&serverTimezone=UTC
    username: ${USER_ROOT}
    password: ${USER_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: false


